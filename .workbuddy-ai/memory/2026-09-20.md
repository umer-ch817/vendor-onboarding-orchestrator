# 2026-09-20

## User question: is an OpenAI-compatible endpoint needed? (answer: no)

User supplied an OpenAI-compatible proxy at `http://localhost:20128/v1`, key
`sk-42f9...`, model `auto/best-free`, and hit
`ECONNREFUSED ::1:20128` from n8n. Investigated:

- **No AI/LLM node exists in any of the 4 n8n workflows.** Node types are only
  httpRequest, code, set, if, switch, webhook, scheduleTrigger, errorTrigger.
  The credential error came from something the user added themselves in the n8n UI.
- **The proxy is alive and the key works** — `/v1/models` returns 401 without the
  key, 4710 models with it, and `auto/best-free` is present.
- **Why it fails from n8n anyway:** n8n runs in Docker. `localhost` inside the
  container is the container itself (and Node resolves it to `::1` first). The
  compose file already uses `host.docker.internal:8000` for the backend for
  exactly this reason. From a container you must use `host.docker.internal`, not
  `localhost`.
- **The backend's LLM is optional and defaults to mock.** `LLM_PROVIDER=mock` in
  `.env`; `backend/app/ai/provider.py:get_provider()` falls back to MockProvider
  when no key is set. `AI_RISK_ANALYSIS` audit events come from the mock, so the
  workflow works with zero network and zero key.
- **The backend cannot use a custom base_url as written.** `OpenAIProvider` builds
  `AsyncOpenAI(api_key=..., timeout=...)` with no `base_url` — it always targets
  api.openai.com. Wiring the user's proxy requires adding an `OPENAI_BASE_URL`
  setting and passing `base_url=`. Also note `json_mode` defaults to True, which
  sends `response_format: {"type":"json_object"}` — not every proxy honours it.
  The backend runs natively on the host (not in Docker), so `localhost:20128`
  *would* be reachable from it.

## Triggering: the backend does NOT call n8n

Confirmed again — nothing reads `N8N_WEBHOOK_URL`. The orchestrator must be
entered by POSTing to the n8n webhook. Re-verified live today:
1. `POST /api/vendors/` then `POST /api/onboarding/` created vendor 21 / case 21
   (VO-2026-41C7AF) — **audit trail empty**. Creating a case does not start anything.
2. `POST http://127.0.0.1:5678/webhook/vendor-onboarding/case` with
   `{"case_id":21,"vendor_id":21,"action":"submit"}` → `{"message":"Workflow was started"}`
3. Audit then showed the full chain: WORKFLOW_TRIGGERED → ASSESSMENT_COMPLETED
   (1 finding, risk 15/100) → AI_RISK_ANALYSIS → CASE_ROUTED (blocked).
Note the audit endpoint is `/api/audit/case/{id}`, not `?entity_type=`.

## Workflow shape: why 4 and not 1

n8n allows exactly one trigger per workflow. The four have four different
triggers, so they cannot be merged:
- Case Orchestrator (28 nodes) — webhook `/vendor-onboarding/case`
- Document Intake (13 nodes) — webhook `/vendor-onboarding/document`
- Error Handler (7 nodes) — errorTrigger
- SLA Escalation (9 nodes) — scheduleTrigger

## Wired up the real AI (user's OpenAI-compatible gateway)

Added `OPENAI_BASE_URL` to `Settings` and `OpenAIProvider` (was hardcoded to
api.openai.com). Client is built with `base_url` only when set, so the SDK
default is preserved. Added a `json_mode` fallback: if a gateway 400s on
`response_format`, retry once without it and let `parse_json()` find the object.

**Three obstacles, in the order hit:**
1. `localhost` resolved to `::1` (proxy listens on IPv4 `0.0.0.0` only) — not
   actually the blocker; `127.0.0.1` also failed.
2. **The real blocker: `HTTP_PROXY`/`HTTPS_PROXY` are set in this environment.**
   httpx forwarded the loopback request to the proxy → `TooManyRedirects`.
   Fixed with `_targets_loopback()` in provider.py: when the base URL is
   loopback, build the client with `httpx.AsyncClient(trust_env=False)`.
3. `auto/best-free` is advertised by the gateway but fails upstream (403
   OpenCode free tier / 429 Felo). Probed alternatives; `auto`, `auto/best-fast`
   and `auto/best-coding` all work. Settled on `auto/best-fast`.
   The gateway routed it to `gemini-3.7-flash-high`; ~12-22s per call.

## Two genuine bugs found only by running it for real

- **Document upload was broken: HTTP 400 on every upload.**
  `logger.info(..., extra={"filename": ...})` — `filename` is a reserved
  LogRecord attribute, so makeRecord raised KeyError. 5 log call sites fixed
  (`filename` → `document_filename` in extraction.py x4, documents.py x1) plus
  `"message"` → `"incident_message"` in webhooks.py. Reserved-attr list is now a
  single constant `RESERVED_LOG_RECORD_ATTRS` in utils/logging.py, shared by the
  JSONFormatter and a new AST-scanning regression test. Note the ~10 other
  `"filename"` keys in rules/engine.py are `evidence=` data dicts, not log extras
  — leave them alone.
- **n8n HTTP nodes 404'd.** `Process Document` / `Verify Extraction` /
  `Submit Case` / `Run Assessment` built URLs from `$json.document_id` /
  `$json.case_id`, but they are chained *after* a callback node whose payload is
  `{recorded, event_id, ...}` — no id. URL became `/api/documents//process`.
  Fixed to `$('Validate Trigger').first().json.<id>`. Also raised the shared
  `timeout_ms` default 30000 → 180000 (a real model takes ~40s per document; the
  30s limit produced `RETRY_EXHAUSTED` for requests that were still working).

## Real-world end-to-end test (vendor 22 / case VO-2026-34EEEC)

- Real company data from Wikipedia REST API (Flex Ltd., Singapore).
- Real document from the internet: IRS Form W-9 PDF, 6 pages, 140 KB.
  NB: **curl to /tmp silently writes 0 bytes in this sandbox** — write downloads
  into the workspace, not /tmp.
- Deliberately messy intake packet (name variants, TBD tax id, SG bank with US
  country, 21-char IBAN, SGD/USD mismatch, ambiguous 03/04/2024 date, conflicting
  headcount 172k vs 200k, adverse media, unapproved net-15 terms, missing
  attachments).
- Real AI classified the packet as `supplier_questionnaire`, confidence 0.50,
  and produced 10 warnings that caught nearly every planted defect.
- Real AI classified the W-9 as `w9`, confidence 1.00, `tin_present: false`,
  "form is blank and contains no completed vendor information".
- Orchestrator: risk 53/100 HIGH, route **blocked**, AI verdict
  **REQUEST_INFORMATION**, 4 signals / 3 open exceptions.
- Recipe for re-running: import → publish → `docker restart vendor-n8n`.

## Debugging note
uvicorn `--reload` does not pick up edits in this OneDrive folder — restart the
process manually after editing backend code.

## Error Handler was orphaned -- wired it up (uncommitted, unverified)

Proved executions 14 (doc 111 via workflow) success and 15 (deliberate bad
payload) status `error`, but **nothing handled failure 15**. Root cause: no
workflow set `settings.errorWorkflow`, so the Error Handler was imported and
active but nothing ever invoked it. n8n does not auto-attach it.

Fix in `n8n/build_workflows.py`:
- `ERROR_WORKFLOW_NAME = "Vendor Onboarding — Error Handler"` and
  `ERROR_WORKFLOW_ID = _workflow_id(ERROR_WORKFLOW_NAME)` — because ids are
  uuid5(NAMESPACE_URL, "vendor-onboarding/" + display name), the id is knowable
  before import. The old comment in workflow_case_orchestrator claiming the id
  "is assigned by the n8n instance on import" was wrong and has been deleted.
- New `_settings(on_error=True)` helper; the three real workflows get
  `errorWorkflow`, the handler gets `on_error=False` (else a failure loops).
- Regenerated, re-imported all 4, published, restarted. All 4 active.
- NOT yet re-tested: the deliberate-failure trigger has not been re-run since.

## Answered: does the UI need n8n? (no)

- `frontend/src` has zero n8n references — it only reads the backend. The only
  "n8n" in the UI is an actor label in the audit timeline.
- The backend already exposes `POST /api/onboarding/{id}/submit` and
  `/{id}/assess` — the *same* endpoints the n8n workflow calls. So the backend
  can run the whole assessment alone; n8n adds approvals, notifications, SLA
  escalation and error handling on top.
- `http://localhost:8000/docs` (Swagger, 200) is a clickable way to run
  everything with no n8n and no curl. Frontend runs on :5173.
- `N8N_WEBHOOK_URL` already exists in config but nothing reads it — that is the
  natural place to hang a "start onboarding" button if the UI should trigger n8n.

## Correction: the UI already had submit/assess buttons

I told the user the website had no buttons that start anything. That was wrong.
`frontend/src/pages/VendorDetailPage.tsx` already renders "Run assessment"
(`onboardingApi.assess`) and "Submit case" (`onboardingApi.submit`) in the page
header, and `onboardingApi` already had both methods. They were simply never
surfaced in conversation because I only grepped for n8n references.
Lesson: check what the UI can already do before proposing to build it.

What was genuinely missing was a trigger for the *full* n8n orchestration
(submit + assess + approvals + notifications + workflow audit events). Built:
- `POST /api/onboarding/{case_id}/start` in backend/app/api/onboarding.py.
  Forwards to `{N8N_WEBHOOK_URL}/webhook/vendor-onboarding/case`. This is the
  ONLY outbound call to n8n in the codebase -- updated the RUNBOOK line that
  said "the backend does not call n8n".
- `onboardingApi.start(id)` in frontend/src/utils/api.ts and a "Start
  onboarding" button in VendorDetailPage (primary; submit demoted to secondary).
- Verified: fresh case 23 (vendor 23 "Demo Runner Pte Ltd") ->
  WORKFLOW_TRIGGERED / ASSESSMENT_COMPLETED / AI_RISK_ANALYSIS / CASE_ROUTED,
  risk 30, status blocked. **No approvals were created** -- correct, because
  the dispatch table maps `blocked` to no approval. A demo that wants to show
  the approval queue needs a case that routes to compliance/senior review.

## Environment gotchas hit today
- `npx tsc --noEmit` and `npm run build` die with "Fatal process out of memory:
  Zone" while the vite dev server is running, regardless of `--max-old-space-size`.
  Stop the dev server (port 5173), run tsc, restart it. tsc then passes clean.
- I killed the dev server to do this and the user saw the frontend as down.
  Warn before stopping a service the user may be looking at.

## ON HOLD -- waiting for "hardened specs" from the user

The layman-first redesign plan (phases A-D: launcher scripts, 4-phase statuses,
Story view, what-happens-next panel, AI visibility, jargon help, worklist
dashboard, first-run tour) is **approved in principle but not to be started**.
The user is waiting on firmer specifications before any of it gets built.

Nothing has been implemented. Working tree is clean apart from this memory file.
Last commit: 3901669 (Start onboarding button). Stack is up (backend 200,
frontend 200).

When they come back with specs, the four open decisions still unanswered are:
1. Scope (A only / A+B / everything)
2. Case page: full rebuild vs. keep six tabs + add the next-action panel
3. Demo pressure (none for now)
4. Real AI vs mock for demos

---

## Resumed: implementing Phase A + B (user pasted the full A-D plan)

### Backend crash diagnosis (user reported asyncpg connection_lost at startup)
Two causes, not one:
- Proximate: Docker Desktop restart took Postgres down; uvicorn's WatchFiles
  reloader picked that moment to restart the app, so lifespan's engine.begin()
  hit a dead database and uvicorn exited permanently.
- Underlying: the machine has 8 GB RAM with ~96 MB free. "Memory Compression"
  holding 2.2 GB. Same root cause as the earlier tsc OOM and the Go OOM that
  killed a `docker ps` diagnostic.

Fix: `initialise_database()` in backend/app/main.py retries 15x x 2s, logs
`database_not_ready` per attempt, then raises a message naming the cause.
scripts/start-dev.sh now runs uvicorn with `--reload-dir backend/app`.
3 new tests -> 22 passed. Verified against real Postgres.

### Phase A status
- 1.1 start.bat, 1.2 stop.bat: done, tested (idempotent + cold start).
- 1.3 setup.bat: done, ran end to end (exit 0). setup.ps1 NOT written.
  Bug fixed: `popd` resets errorlevel, so the npm-failure check never fired.
  Added: `where npm` guard.
- 1.4 README: rewritten as layman quickstart. Also fixed a `cd backend` trap in
  ARCHITECTURE.md and docs/workflow.md (breaks .env resolution).
- 1.5 ensure_workflows.py: verified no-op when all 4 workflows exist.
- New: `.env` is mandatory. Without it config falls back to Docker hostnames
  (`postgres:5432`) and the native backend cannot reach the database. setup.bat
  now copies .env.example -> .env; start.bat stops with a clear message if
  .env is missing.

### Not yet done / tested
setup.ps1 is written and parses clean, but only its FAILURE path was exercised:
this sandbox's PowerShell session has a trimmed PATH (Get-Command python -> not
found, though cmd finds it) and blocks nested `powershell`. Also note
Write-Host cannot be captured by redirection in PS 5.1 -- use Start-Transcript
to capture it.

The setup.bat "no venv detected" path was never exercised either -- only the
already-installed path ran, because the backend/frontend were up and the user
did not want them disturbed.

Base interpreter for backend/.venv is a uv-managed CPython 3.12.14 at
C:\Users\umer2\AppData\Roaming\uv\python\cpython-3.12.14-windows-x86_64-none,
which is NOT on PATH.

---

## Phase B, C and D all built (user said auto-advance, test each phase)

Copy decisions I made when they said "idk do what's better":
- Phases: **Set up -> Collect -> Score -> Sign-off**. "Score" because the
  artefact is a number out of 100; "Sign-off" because the phase contains
  `rejected` and "Approve" would lie.
- `rejected` / `onboarding_complete` are an **end-state badge**, not a finished
  4th step - a stepper implies progress, and a declined case did not advance.
- `blocked` lives in Collect, not a 5th phase: all three blocking codes are
  document/tax issues.

### New files (frontend)
- utils/phases.ts - 12 statuses -> 4 phases. BUG FOUND AND FIXED: `submitted`
  initially rendered identically to `draft`, so pressing Submit visibly did
  nothing. Added PHASE_CLOSING = {'submitted'}.
- utils/nextStep.ts - "what happens next" copy per status + risk band sentences.
- utils/story.ts - plain-language sentence per audit event (18 mapped), with an
  isAi flag for AI_RISK_ANALYSIS / AI_UNCERTAINTY.
- utils/glossary.ts - 8 jargon terms translated.
- components/ui/PhaseStepper.tsx + .css
- components/ui/NextStepPanel.tsx + .css (also carries 2.4 risk explanation and
  the 2.5 AI note)
- components/ui/StoryTimeline.tsx + .css (story + collapsed glossary)
- components/ui/FirstRunTour.tsx + .css (dismissible, localStorage)
- pages/DashboardPage.tsx rewritten: worklist FIRST with "why it is waiting",
  4 headline metrics, translucent distribution bars grouped by phase (not raw
  status), secondary metrics + definitions collapsed.

### Verification done
- tsc --noEmit: exit 0 (three times, after each phase).
- Vite transform: every changed module HTTP 200.
- Logic executed in node against the real transformed modules: all 12 statuses,
  next-step copy for all 12 + unknown, 18 story events + fallbacks.
- Backend pytest: 22 passed. Backend 200, frontend 200 throughout.

### Known limitation (worth fixing)
2.5 cannot distinguish "AI was unavailable" from "AI never ran": the API does
not expose `ai_degraded` on the assessment, only `ai_model`. Showing the honest
weaker wording instead. Persisting ai_degraded would fix it properly.

### Answers given before Phase B
1. The "Please continue" message WAS the app, not the user. (Asking was right.)
2. Demo timing: the user records the demo themselves, no fixed date.
3. **Real AI will be running in the demo** (their API key is in play). This makes
   AI visibility/credit more important than the Phase C parking suggests.
4. Copy-first for 2.1-2.4: write wording, get reaction, then build.

### Phase B copy written: docs/ui-copy.md
Plain-language wording for the four phases, Story sentences per event,
"what happens next" per status, risk bands. All values sourced from code.

Key finding while writing it: **item 2.4 already exists in the backend.**
risk_scoring.py:166 `_explain()` builds a full sentence ("Risk score is 53/100,
placing this vendor at HIGH risk. The score is the sum of 4 identified
factors: ..."), risk_service.py:92 returns it, and RiskReviewPage.tsx:305 +
VendorDetailPage.tsx:477 already render it. So 2.4 is presentation work, not new
writing -- promote it to the default view.

Statuses (12): draft, submitted, document_collection, extraction, validation,
risk_analysis, review_required, approval_pending, approved, rejected,
onboarding_complete, blocked.
Routes: auto_approve / compliance_review / senior_review / escalate / blocked.
Bands: low<=25, medium<=50, high<=75, critical>75. Auto-approve ceiling 25.
Blocking codes: DOCUMENT_MISSING, EXTRACTION_FAILED, TAX_INFORMATION_MISSING.
All three blocking codes are document/tax issues, so `blocked` maps to the
Collect phase rather than being a fifth phase.

---

## Real-world end-to-end Playwright test (vendor 27, Northwind Precision)

User asked for: real-world test with good data, Playwright, screenshots, outside
the project folder, every UI feature + AI exercised. Built at
`C:/Users/umer2/vendor-onboarding-e2e/` (sibling of the project, not inside).

Fixtures: 5 plain-text documents for a US supplier (W-9, COI $5M, IL
business-registration, BMO Harris banking letter, supplier questionnaire).
Uploaded as `.txt` because the seed/upload path accepts text/plain and seed
intentionally uses `.txt` (naming `.pdf` would route through the PDF parser
which cannot read text).

Flow exercised end to end through the UI: vendor create (API) -> open case ->
upload 5 docs (one as `other` so the classifier decides it's a W-9) -> Extract
each -> Submit -> Run assessment -> Start onboarding (n8n). 20 screenshots in
`screenshots/01-…` through `20-…`.

**Result: 25/26 checks passed, 2 real bugs surfaced.**

### Real bugs found (only visible by running with real data)

1. **Country normalization missing.** `requirement_service.py:203`
   `(country or "").upper() == "US"` — literal string compare. I sent
   `country: "United States"` (which is what a human would type), so the
   REQ-TAX-CERT guard never fired and a US vendor was wrongly asked for a
   "Tax registration certificate" -> DOCUMENT_MISSING -> case blocked.
   Same shape at line 205 for REQ-W9 (would wrongly *suppress* W-9 for a US
   vendor named "United States"). Fix: normalize "United States", "USA",
   "U.S.A.", "US" to "US" before the compare (or accept any of them).
   This is the test's main catch.

2. **ADDRESS_MISMATCH false positive.** Same vendor address (vendor record
   == W-9 == business-registration == banking letter all match the W-9 text)
   triggered `Address differs between documents` with `severity: low`.
   The compare likely compares per-field vendor record against extracted
   multi-line string without normalising. Mild severity but it's noise and
   it adds to the open-issues counter shown in the UI.

### Third exception is by-design, not a bug

`INCOMPLETE_OWNERSHIP_INFORMATION` — supplier questionnaire didn't include a
`beneficial_owners` field. The questionnaire fixture I wrote omitted it; the
requirement is correct (FinCEN CTA-style disclosure). Not an app defect; just
an incomplete fixture.

### What worked (no defects)
- Real AI ran end to end. `ai_model=gemini-3.7-flash-high`, ~12-22s per call
  through the `auto/best-fast` gateway. UI shows `AI SUGGESTION` block with
  model name and the AI's recommended action; Story timeline renders the
  "The AI reviewed this case" line with the AI chip.
- W-9 was uploaded as type `other`; classifier correctly returned `w9` (the
  upload-type dropdown does NOT list `w9` — intentional, the AI classifies
  ambiguous uploads). Extraction then pulled the EIN, address, name.
- All 5 documents extracted successfully (5 LLM calls + 1 classifier call).
- Story tab is default; PhaseStepper shows BLOCKED on Collect; NextStepPanel
  shows "What happens next / Waiting on you / 3 open issues / Risk 35/100
  High / Required document not received +15 / Beneficial ownership not
  disclosed +15 / Address inconsistency +5" + the fixed-rules disclaimer +
  AI suggestion. All renders.
- Dashboard distribution bars: only chart in the app, opacity 0.7 over an
  rgba(17,24,39,0.06) track — translucent as required. Zero chart library
  imports (`package.json` has no chart/d3/recharts/echarts), zero canvas /
  recharts-wrapper in DOM.
- First-run tour shows on a fresh browser context.
- Every page (/, /cases, /documents, /risk, /exceptions, /approvals, /audit,
  /settings) renders without errors. Zero uncaught console errors.

### Test harness notes for next time
- The vendor-creation form does NOT exist in the UI; only the create-case
  form exists, which selects from an existing vendor. So fixtures must
  POST to `POST /api/vendors/` first.
- API base is `/api` (no v1). Backend routes: `/api/vendors`,
  `/api/onboarding`, `/api/documents`, `/api/risk/case/{id}/assessment`,
  `/api/onboarding/{id}` for full detail.
- Risk assessment response keys: `overall_score`, `risk_level`,
  `risk_factors`, `reasoning`, `recommended_action`, `ai_model`,
  `confidence` — NOT `score_total`/`route`/`explanation`. The detailed
  `_explain()` sentence lives on the Risk tab (`/risk/case/{id}`), not on
  the case detail — only the chip-style breakdown shows on the detail.
- The case list search "Search case number or vendor…" did not match vendor
  legal_name server-side (only case_number). Navigating straight to
  `/cases/{id}` is more reliable in the script.
- Per-run, this creates a new vendor + case (no idempotency). 27 runs left
  27 vendors + 27 cases in the DB. Acceptable for a test; a cleanup step
  could DELETE the created vendor at end of run if desired.

---

## Committed (was 30 files sitting uncommitted)

Split into three commits rather than one blob, oldest work first:
1. **Runnability** (Phase A): setup/start/stop .bat + setup.ps1, .env
   creation, README/RUNBOOK/ARCHITECTURE/docs rewrite, the
   `initialise_database()` retry fix for the reported startup crash,
   `--reload-dir`, ensure_workflows.py, 3 new regression tests, and
   untracking the two vite.config.ts.timestamp-*.mjs artefacts (now also
   gitignored via `vite.config.ts.timestamp-*.mjs` so they cannot return).
2. **UI legibility** (Phases B/C/D): all of frontend/src (phases.ts,
   nextStep.ts, story.ts, glossary.ts, PhaseStepper, NextStepPanel,
   StoryTimeline, FirstRunTour, DashboardPage rewrite) + docs/ui-copy.md.
3. **Working notes**: this file and MEMORY.md.

Diff was reviewed before staging: no secrets (the one `api_key=` hit in
test_regressions.py is the literal "test-key"), .env is gitignored and was
never staged.

## Next, in the order I recommended (user picked "commit first")
1. ~~Commit~~ done.
2. Fix `requirement_service.py:203` country normalisation (US vendor asked
   for a tax certificate -> case blocked). ~10 lines + regression test.
3. Fix ADDRESS_MISMATCH false positive (vendor address == W-9 address but
   still flagged).
4. Optional: queue-ageing bands chart on the dashboard (4 bands,
   translucent, client-side only -- no backend change needed).
5. Longer term: persist ai_degraded so the UI can distinguish "AI
   unavailable" from "AI never ran".

Charts note: the dashboard API returns only 9 scalars, no time series, and
`avg_onboarding_time_days` / `automation_rate` are null with
`completed_this_month: 0`. Any trend-over-time chart would need new
backend aggregation AND would render empty in a demo. So trend charts are
deferred, not just unbuilt.

---

## Country bug fixed + dashboard redesigned

Three commits: country fix, backdate + waiting_by_age, dashboard redesign.

### Country fix

`requirement_service.py:203` and `:205` compared country to the literal
two-letter code, so a US vendor recorded as "United States" was wrongly
asked for the non-US tax certificate (DOCUMENT_MISSING -> blocked) and
never asked for a W-9. Fixed by adding `app/utils/countries.py`
(`normalise_country()` + `COUNTRY_ALIASES`) used both by the requirement
resolver and by `HighRiskGeographyRule`. The rules engine's own
`HIGH_RISK_COUNTRIES = {"KP","IR","SY","CU","RU","BY","MM","AF"}` differs
from requirement_service's `{"RU","BY","IR","KP","SY","CU","VE"}` -- two
divergent watchlists, not just a normalisation issue. Did not unify
because changing which countries are flagged is a policy decision; flagged
it for the user. 6 new regression tests, suite at 28 passing.

The `app/utils` location matters: `app.rules.__init__` imports the engine
and `requirement_service` imports from `app.rules.types`, so importing
directly between requirement_service and the engine would form a cycle.
`app.utils` has no rules dependency.

### Backdated seed + waiting_by_age

The seed script was spreading dates *inside document text* but never set
case timestamps, so every install looked like it was switched on this
morning. Any view of age or throughput collapsed to a single bar. Fixed
by `backdate_demo_timelines()` in `backend/scripts/seed_demo_data.py`,
which spreads `created_at` over the past ~21 days, picks a `progress`
fraction for `updated_at`, backdates vendors (1-25d before their case),
documents (5-60% into the case window), audit events (kept in id order so
the Story timeline still reads forwards), and risk assessments. Runs as
the last step before commit, so it only moves timestamps -- never the
state computed above it. Also added `--backdate-only` to the entry point
to refresh history without a full reseed.

Then added `waiting_by_age` aggregation to the dashboard API: 4 buckets
(<1d, 1-3d, 3-7d, 7+d) computed over every case in a review status (not
just the top-10 worklist), measured from `updated_at`. Result with the
backdated dataset: 2/3/10/8 across the four bands -- the chart now
shows real staleness.

### Dashboard redesign

- New `Charts.tsx` / `Charts.css`: a single `ColumnChart` component
  (vertical translucent columns) with no chart library. Same
  design language as the existing bars.
- DashboardPage rewritten to: keep worklist first, 4 metric cards, then
  a grid-3 of charts (Cases by stage / Risk distribution / How long they
  have waited). "Issues by severity" moved into the collapsed "More
  numbers" section so the top of the page stays minimal. Definitions
  stay collapsed. Subtitle on the ageing chart says "23 cases are waiting
  on a person" so the chart is not just a decoration.
- Verified: tsc clean (exit 0), all changed modules transform HTTP 200
  through Vite, computed opacities (0.72 columns, 0.7 bars), no canvas
  in the DOM, no console errors. Zero chart library imports anywhere.

### Backend restart gotcha

uvicorn --reload does not pick up edits in this OneDrive folder
(memory note already recorded). After backend code changes I had to
kill PID 25632 and restart with the exact command from
scripts/start-dev.sh. The `nohup ... &` form inside a Bash tool call
DIDN'T survive past the call returning -- the child died. The fix was
to use the Bash tool's `run_in_background: true` instead, which keeps
the server alive as a managed task (task_id P985Ab).

Also: the sandbox's HTTP_PROXY intercepts curl to 127.0.0.1 unless
`--noproxy '*'` is passed. Symptom is 502 "upstream connect failed".
Everything in this session now uses `--noproxy '*'` for local curls.
